% Report del progetto S.A.G.E.: struttura, analisi, progettazione e implementazione.
% Per rigenerare il PDF basta compilare con pdflatex o latexmk -pdf.

\documentclass[11pt,a4paper]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[italian]{babel}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{geometry}
\geometry{margin=2.5cm}
\usepackage{graphicx}
\usepackage[hidelinks]{hyperref}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{float}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{enumitem}

% Stile dei blocchi SQL nel report: colori leggibili e numeri di riga.
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codeblue}{rgb}{0,0,0.8}

\lstset{
    language=SQL,
    backgroundcolor=\color{backcolour},
    commentstyle=\color{codegreen},
    keywordstyle=\color{codepurple}\bfseries,
    numberstyle=\tiny\color{gray},
    stringstyle=\color{orange},
    basicstyle=\ttfamily\small,
    breakatwhitespace=false,
    breaklines=true,
    captionpos=b,
    keepspaces=true,
    numbers=left,
    numbersep=5pt,
    showspaces=false,
    showstringspaces=false,
    showtabs=false,
    tabsize=2,
    morekeywords={LAST_INSERT_ID, ON, DUPLICATE, KEY, UPDATE, LIMIT, COALESCE, NULLIF, BOOLEAN, TRUE, FALSE}
}
\setlist{nosep, leftmargin=*}

% Dati del frontespizio, separati in comandi per modificarli in fretta.
\newcommand{\ProjectTitle}{S.A.G.E. - Sistema di Analisi e Gestione Economica}
\newcommand{\CourseName}{Corso: Basi di Dati}
\newcommand{\AcademicYear}{Anno accademico 2025/2026}
\newcommand{\GroupMembers}{Antonio Scharmuller \\ Riccardo Dallacasa}
\newcommand{\StudentNumbers}{Matricola 000000 \\ Matricola 000000}
\newcommand{\Emails}{antonio.scharmuller@studio.unibo.it \\ riccardo.dallacasa@studio.unibo.it}
\newcommand{\Supervisor}{Docente: Nome Cognome}
\newcommand{\UniversityLogo}{logo-unibo.png}

\begin{document}

\hypersetup{pageanchor=false}
\begin{titlepage}
    \centering
    \vspace*{1.5cm}
    {\Huge\bfseries \ProjectTitle \\[1.5em]}
    \vfill
    {\Large \CourseName \\}
    {\Large \AcademicYear \\}
    \vspace{1.5cm}
    {\large\bfseries Componenti del gruppo}\\
    \GroupMembers\\[1em]
    {\large\bfseries Matricole}\\
    \StudentNumbers\\[1em]
    {\large\bfseries Email universitarie}\\
    \Emails\\[1.5em]
    {\large \Supervisor}\\[2em]
    % Logo opzionale: basta mettere il file nella cartella o aggiornare il percorso.
    % \includegraphics[height=3cm]{\UniversityLogo}
    \vfill
    {\large \today}
\end{titlepage}

\clearpage
\hypersetup{pageanchor=true}
\pagenumbering{roman}
\tableofcontents
\newpage
\pagenumbering{arabic}

\section{Introduzione}
\subsection{Obiettivo del progetto}
S.A.G.E. (Sistema di Analisi e Gestione Economica) è un'applicazione desktop per la gestione delle finanze personali di uno studente universitario. Il sistema supporta la registrazione di entrate e spese, la classificazione dei movimenti, la definizione di budget e la consultazione di riepiloghi utili per monitorare l'andamento economico nel tempo.

L'obiettivo del progetto non è soltanto realizzare un'applicazione funzionante, ma documentare l'intero ciclo di progettazione di una base di dati: analisi dei requisiti, modellazione concettuale, traduzione logica, implementazione SQL e collegamento a un programma Java tramite JDBC.

\subsection{Descrizione generale del sistema}
S.A.G.E. è progettato come sistema multiutente. Ogni studente accede con le proprie credenziali e visualizza soltanto i dati collegati alla propria email. Il portafoglio personale comprende transazioni, categorie, fonti, tag, budget, documenti digitali e modelli di spese ricorrenti.

L'applicazione è realizzata in Java Swing e comunica con un database MySQL tramite JDBC. La logica applicativa è organizzata in livelli: le classi del package \texttt{model} rappresentano il dominio, i DAO incapsulano le query SQL, i controller coordinano i casi d'uso e le classi del package \texttt{view} compongono l'interfaccia grafica.

\subsection{Ambito del sistema}
L'ambito del progetto comprende:
\begin{itemize}
    \item gestione individuale del portafoglio economico personale;
    \item registrazione di spese ed entrate;
    \item classificazione delle spese tramite categorie e tag;
    \item classificazione delle entrate tramite fonti;
    \item definizione di budget mensili e budget specifici per categoria;
    \item gestione di spese ricorrenti come modelli astratti;
    \item associazione di documenti digitali alle spese;
    \item visualizzazione di riepiloghi e statistiche.
\end{itemize}
Non rientrano nell'ambito del sistema la condivisione di conti tra utenti, la sincronizzazione cloud dei file allegati e l'integrazione con servizi bancari esterni.

\subsection{Utenti e ruoli previsti}
Il sistema prevede due ruoli:
\begin{itemize}
    \item \textbf{Utente}: registra transazioni, consulta il proprio portafoglio, gestisce categorie personali, definisce budget e associa documenti alle spese.
    \item \textbf{Amministratore}: gestisce elementi di sistema, come categorie, fonti e tag globali. L'amministratore può accedere solo a statistiche aggregate e non ai dettagli finanziari personali degli utenti.
\end{itemize}

\subsection{Tecnologie utilizzate}
\begin{itemize}
    \item \textbf{DBMS}: MySQL.
    \item \textbf{Linguaggio applicativo}: Java.
    \item \textbf{Interfaccia}: Java Swing.
    \item \textbf{Connessione al database}: JDBC con MySQL Connector/J.
    \item \textbf{Architettura}: applicazione desktop 2-tier con accesso diretto al DBMS.
\end{itemize}

\subsection{Scelte progettuali preliminari}
Le scelte iniziali più rilevanti sono:
\begin{itemize}
    \item isolamento dei dati tramite email dell'utente autenticato;
    \item distinzione tra elementi globali di sistema ed elementi personali;
    \item gestione opzionale del budget, che non blocca l'inserimento di nuove spese;
    \item memorizzazione dei documenti digitali tramite percorso file, evitando di salvare il contenuto binario direttamente nel database.
\end{itemize}
La scelta sui documenti riduce il peso del DBMS e semplifica le operazioni JDBC. Presenta però un limite noto: se l'applicazione viene usata da un altro dispositivo collegato allo stesso database, un percorso locale potrebbe non essere più valido. Questa criticità viene considerata tra i possibili sviluppi futuri.

\section{Analisi dei requisiti}
\subsection{Requisiti in linguaggio naturale}
Il sistema deve permettere a uno studente di registrare e monitorare i propri movimenti economici. Ogni transazione deve avere data, importo e descrizione. Una spesa deve essere associata a una categoria e può avere tag e documenti digitali; un'entrata deve essere associata a una fonte. L'utente deve poter definire budget per periodo e, opzionalmente, per categoria. Il sistema deve inoltre supportare spese ricorrenti come modelli da cui generare movimenti effettivi.

\subsection{Analisi delle ambiguità e decisioni progettuali}
\begin{longtable}{p{3.5cm} p{5cm} p{6cm}}
\toprule
\textbf{Concetto} & \textbf{Ambiguità} & \textbf{Decisione progettuale} \\
\midrule
\endfirsthead
\toprule
\textbf{Concetto} & \textbf{Ambiguità} & \textbf{Decisione progettuale} \\
\midrule
\endhead
Periodo temporale & Come collegare transazioni e budget mensili? & Il periodo è derivato dalla data della transazione e storicizzato nella tabella \textsc{PERIODO}. \\
Transazione & Separare fisicamente spese ed entrate? & Si usa una relazione unica \textsc{TRANSIZIONE} con attributo \texttt{TipoTransazione}. \\
Classificazione & Come distinguere catalogo comune e personalizzazioni? & \textsc{CATEGORIA}, \textsc{TAG} e \textsc{FONTE} usano \texttt{is\_system} ed eventuale \texttt{Email\_Proprietario}. \\
Spesa ricorrente & È una transazione già avvenuta? & No: è un modello generatore che può produrre transazioni future. \\
Documenti & Salvare file o riferimento? & Il DB salva percorso, tipo e data acquisizione. Il file resta nel file system, non in un BLOB. \\
Privacy & L'admin vede i movimenti personali? & No: l'accesso amministrativo è limitato a dati aggregati. \\
\bottomrule
\end{longtable}

\subsection{Specifiche ristrutturate}
\begin{itemize}
    \item R1: autenticare utenti e amministratori.
    \item R2: registrare spese ed entrate con data, importo e descrizione.
    \item R3: associare ogni spesa a una categoria e ogni entrata a una fonte.
    \item R4: associare tag e documenti digitali alle spese.
    \item R5: gestire budget mensili e budget per categoria.
    \item R6: gestire spese ricorrenti come modelli astratti.
    \item R7: filtrare ogni dato operativo tramite l'email dell'utente autenticato.
    \item R8: consentire all'amministratore solo operazioni su cataloghi globali e statistiche aggregate.
\end{itemize}

\subsection{Glossario dei termini essenziali}
\begin{itemize}
    \item \textbf{Utente}: studente identificato da email, nome, cognome, password e ruolo.
    \item \textbf{Transazione}: movimento economico generico, distinto in spesa o entrata.
    \item \textbf{Categoria}: classificazione principale di una spesa.
    \item \textbf{Fonte}: origine di un'entrata.
    \item \textbf{Tag}: etichetta trasversale che consente classificazioni aggiuntive.
    \item \textbf{Budget}: limite di spesa collegato a utente, periodo ed eventuale categoria.
    \item \textbf{Documento}: riferimento digitale a uno scontrino o a una fattura associata a una spesa.
    \item \textbf{Spesa ricorrente}: modello da cui generare spese periodiche.
\end{itemize}

\subsection{Classi di utenza e viste del sistema}
\begin{itemize}
    \item \textbf{Studente}: utilizza dashboard, transazioni, budget, documenti, categorie personali e report.
    \item \textbf{Amministratore}: gestisce cataloghi globali e consulta indicatori aggregati.
\end{itemize}
La vista studente è centrata sul portafoglio personale. Ogni schermata applicativa riceve l'utente corrente e usa la sua email come chiave di sessione.

\subsection{Elenco delle operazioni principali}
\begin{itemize}
    \item Login e apertura della dashboard personale.
    \item Inserimento di una spesa con categoria, tag opzionali e documento opzionale.
    \item Inserimento di una entrata con fonte.
    \item Consultazione dei movimenti per periodo.
    \item Definizione o aggiornamento di un budget.
    \item Associazione e apertura di un documento digitale.
    \item Creazione di categorie, fonti e tag personali.
    \item Consultazione di riepiloghi e statistiche aggregate.
\end{itemize}

\subsection{Vincoli espliciti e impliciti}
\begin{itemize}
    \item Ogni query sui dati personali deve filtrare per email dell'utente autenticato.
    \item Una spesa deve avere una categoria e non deve avere una fonte.
    \item Una entrata deve avere una fonte e non deve avere una categoria.
    \item I tag sono associabili solo alle spese.
    \item I documenti sono associabili solo alle spese.
    \item Il budget è opzionale e non impedisce l'inserimento di movimenti.
    \item Il contenuto dei documenti non è salvato nel database; viene salvato solo il riferimento al file.
\end{itemize}

\section{Progettazione concettuale}
\subsection{Criteri generali di modellazione}
La progettazione concettuale privilegia un modello chiaro, con entità separate per gli oggetti persistenti principali e associazioni esplicite per i legami molti-a-molti. Le scelte sono guidate da tre criteri: isolamento dei dati per utente, semplicità di interrogazione e coerenza con l'applicazione Java.

\subsection{Entità principali}
\begin{longtable}{p{3.8cm} p{11cm}}
\toprule
\textbf{Entità} & \textbf{Ruolo nel sistema} \\
\midrule
\endfirsthead
\toprule
\textbf{Entità} & \textbf{Ruolo nel sistema} \\
\midrule
\endhead
\textsc{UTENTE} & Identifica studenti e amministratori. L'email è chiave primaria e riferimento per i dati personali. \\
\textsc{TRANSIZIONE} & Rappresenta movimenti economici, sia spese sia entrate, tramite \texttt{TipoTransazione}. \\
\textsc{CATEGORIA} & Classifica le spese; può essere di sistema o personale. \\
\textsc{FONTE} & Classifica le entrate; può essere di sistema o personale. \\
\textsc{TAG} & Aggiunge classificazioni trasversali alle spese. \\
\textsc{PERIODO} & Storicizza mese e anno usati dai budget. \\
\textsc{BUDGET} & Definisce una soglia di spesa per periodo, con eventuale categoria. \\
\textsc{DOCUMENTO} & Conserva percorso, tipo e data di acquisizione del file associato a una spesa. \\
\textsc{SPESA\_RICORRENTE} & Modello astratto per spese periodiche future. \\
\bottomrule
\end{longtable}

\subsection{Raffinamenti dello schema}
\subsubsection{Gerarchia delle transazioni}
Nel dominio esistono due sottotipi di transazione: spesa ed entrata. La specializzazione è totale ed esclusiva: ogni transazione è necessariamente una spesa o una entrata, e non può essere entrambe. Nel modello logico questa gerarchia è collassata in \textsc{TRANSIZIONE}, con l'attributo \texttt{TipoTransazione}. I vincoli CHECK garantiscono la coerenza: una spesa richiede \texttt{ID\_Categoria}, una entrata richiede \texttt{ID\_Fonte}.

\subsubsection{Elementi di sistema e personali}
\textsc{CATEGORIA}, \textsc{TAG} e \textsc{FONTE} sono modellati con una soluzione ibrida. Gli elementi globali hanno \texttt{is\_system = TRUE} e nessun proprietario; gli elementi personali hanno \texttt{is\_system = FALSE} e sono collegati a un utente. Le query applicative mostrano a uno studente sia gli elementi globali sia quelli personali.

\subsubsection{Reificazione del budget}
Il budget è modellato come entità autonoma collegata a utente e periodo. L'eventuale categoria distingue un budget specifico da un budget mensile complessivo. L'attributo \texttt{Totale\_Speso\_Attuale} è ridondante, ma serve a rendere efficiente la dashboard.

\subsubsection{Documenti digitali}
Il documento digitale è collegato a una spesa e contiene \texttt{Path\_File}, \texttt{Tipo\_File} e \texttt{Data\_Acquisizione\_Documento}. Il contenuto binario del file non è salvato nel DBMS.

Questa scelta riduce il peso del database e semplifica backup, query e codice JDBC. Il limite principale è la portabilità: spostandosi su un altro dispositivo, il percorso locale potrebbe non esistere. Una futura versione potrebbe usare path relativi sincronizzati oppure un attributo \texttt{LONGBLOB}, qualora la portabilità completa del documento attraverso il solo database diventasse un requisito prioritario.

\subsection{Associazioni principali e cardinalità}
\begin{longtable}{p{4.2cm} p{3cm} p{7.2cm}}
\toprule
\textbf{Associazione} & \textbf{Cardinalità} & \textbf{Motivazione} \\
\midrule
\endfirsthead
\toprule
\textbf{Associazione} & \textbf{Cardinalità} & \textbf{Motivazione} \\
\midrule
\endhead
Utente--Transazione & 1:N & Un utente può registrare molte transazioni; ogni transazione appartiene a un solo utente. \\
Transazione--Categoria & N:1 opzionale & Solo le spese hanno una categoria. \\
Transazione--Fonte & N:1 opzionale & Solo le entrate hanno una fonte. \\
Spesa--Tag & N:M & Una spesa può avere più tag e un tag può essere riutilizzato. \\
Spesa--Documento & 1:N logico & Una spesa può avere documenti; nella UI corrente si limita a un documento per semplicità. \\
Utente--Budget & 1:N & Ogni utente può definire più budget nel tempo. \\
Periodo--Budget & 1:N & Un periodo può essere condiviso da budget di utenti diversi. \\
\bottomrule
\end{longtable}

\subsection{Vincoli non rappresentabili direttamente nello schema E/R}
Alcune regole richiedono vincoli SQL o controlli applicativi:
\begin{itemize}
    \item una categoria personale è visibile solo al proprietario;
    \item una spesa può usare solo categorie di sistema o categorie personali dell'utente;
    \item un documento può essere collegato solo a una spesa, non a una entrata;
    \item l'amministratore non deve visualizzare dettagli finanziari personali;
    \item l'inserimento di spesa, tag, documento e aggiornamento budget deve essere atomico.
\end{itemize}

\subsection{Dizionario di entità e associazioni}
Il dizionario riassume gli attributi principali:
\begin{itemize}
    \item \textsc{UTENTE}: Email, Password, Nome, Cognome, Ruolo.
    \item \textsc{TRANSIZIONE}: ID, TipoTransazione, Importo, Data, Descrizione, Email, Categoria/Fonte.
    \item \textsc{BUDGET}: ID, Importo\_Limite, Alert\_Soglia, Totale\_Speso\_Attuale, Periodo, Categoria, Email.
    \item \textsc{DOCUMENTO}: ID, ID\_Transizione, Path\_File, Tipo\_File, Data\_Acquisizione\_Documento.
    \item \textsc{SPESA\_TAG}: associazione molti-a-molti tra spesa e tag.
\end{itemize}

\section{Specifiche funzionali}
\subsection{Elenco delle funzionalità richieste}
Le funzionalità principali sono organizzate in operazioni utente e operazioni amministrative. Le prime lavorano sempre sul portafoglio dello studente autenticato; le seconde riguardano cataloghi e statistiche aggregate.

\subsection{Operazioni di inserimento}
\begin{longtable}{p{3cm} p{5cm} p{6.5cm}}
\toprule
\textbf{Operazione} & \textbf{Input} & \textbf{Tabelle coinvolte} \\
\midrule
\endfirsthead
\toprule
\textbf{Operazione} & \textbf{Input} & \textbf{Tabelle coinvolte} \\
\midrule
\endhead
Login & Email, password & \textsc{UTENTE} \\
Nuova spesa & Importo, data, descrizione, categoria, tag, documento opzionale & \textsc{PERIODO}, \textsc{TRANSIZIONE}, \textsc{SPESA\_TAG}, \textsc{DOCUMENTO}, \textsc{BUDGET} \\
Nuova entrata & Importo, data, descrizione, fonte & \textsc{PERIODO}, \textsc{TRANSIZIONE} \\
Nuovo budget & Periodo, importo limite, categoria opzionale & \textsc{BUDGET}, \textsc{PERIODO}, \textsc{TRANSIZIONE} \\
Nuovo documento & Spesa, percorso file, tipo file & \textsc{DOCUMENTO}, \textsc{TRANSIZIONE} \\
\bottomrule
\end{longtable}

\subsection{Operazioni di modifica ed eliminazione}
Le modifiche riguardano principalmente budget, categorie personali, fonti personali e tag personali. Le eliminazioni devono rispettare i vincoli referenziali: ad esempio una categoria personale eliminata non deve lasciare riferimenti incoerenti nelle spese già registrate. La cancellazione di transazioni o documenti sfrutta le regole di integrità definite nello schema SQL.

\subsection{Operazioni di interrogazione}
\begin{itemize}
    \item elenco delle transazioni dell'utente per periodo;
    \item elenco di categorie, fonti e tag disponibili;
    \item elenco dei budget e relativo stato di superamento;
    \item elenco dei documenti associati alle spese dell'utente;
    \item elenco delle spese ancora prive di documento.
\end{itemize}

\subsection{Operazioni statistiche e amministrative}
Le statistiche includono totali mensili di spese ed entrate, rapporto percentuale tra spese ed entrate, budget superati e aggregazioni per categoria. L'amministratore deve utilizzare viste aggregate, evitando l'accesso a descrizioni, documenti o movimenti granulari degli utenti.

\section{Progettazione logica}
\subsection{Stima del volume dei dati}
Per un utilizzo universitario individuale si stimano volumi contenuti ma crescenti nel tempo:
\begin{itemize}
    \item poche decine di utenti demo nel contesto del progetto;
    \item circa 50--150 transazioni annue per utente;
    \item 5--20 categorie personali per utente;
    \item un numero di documenti inferiore o uguale al numero di spese;
    \item pochi budget per mese e per utente.
\end{itemize}
Questi volumi giustificano un modello relazionale normalizzato e indici mirati sulle chiavi esterne e sui campi più filtrati.

\subsection{Descrizione delle operazioni principali e frequenza}
\begin{longtable}{p{4cm} p{3cm} p{7cm}}
\toprule
\textbf{Operazione} & \textbf{Frequenza stimata} & \textbf{Osservazioni} \\
\midrule
\endfirsthead
\toprule
\textbf{Operazione} & \textbf{Frequenza stimata} & \textbf{Osservazioni} \\
\midrule
\endhead
Login & Alta & Prima operazione di ogni sessione. \\
Inserimento transazione & Media & Deve essere atomico con periodo, documento e budget. \\
Consultazione dashboard & Alta & Motiva la ridondanza sul totale speso del budget. \\
Gestione documenti & Media & Lettura frequente, inserimento occasionale. \\
Report aggregati & Bassa/Media & Usati per analisi periodica. \\
\bottomrule
\end{longtable}

\subsection{Schemi di navigazione}
La navigazione principale parte da \textsc{UTENTE}, identificato dalla sessione. Da esso si raggiungono le transazioni, i budget, gli elementi personali e le spese ricorrenti. Le transazioni permettono di navigare verso categorie, fonti, tag e documenti. Questo schema conferma la centralità dell'email come chiave di isolamento.

\subsection{Tabelle degli accessi}
\begin{longtable}{p{4cm} p{5cm} p{5.5cm}}
\toprule
\textbf{Funzione} & \textbf{Tabelle lette} & \textbf{Tabelle scritte} \\
\midrule
\endfirsthead
\toprule
\textbf{Funzione} & \textbf{Tabelle lette} & \textbf{Tabelle scritte} \\
\midrule
\endhead
Login & \textsc{UTENTE} & -- \\
Nuova spesa & \textsc{PERIODO}, \textsc{BUDGET} & \textsc{PERIODO}, \textsc{TRANSIZIONE}, \textsc{SPESA\_TAG}, \textsc{DOCUMENTO}, \textsc{BUDGET} \\
Nuova entrata & \textsc{PERIODO} & \textsc{PERIODO}, \textsc{TRANSIZIONE} \\
Documenti & \textsc{DOCUMENTO}, \textsc{TRANSIZIONE} & \textsc{DOCUMENTO} \\
Dashboard budget & \textsc{BUDGET}, \textsc{PERIODO}, \textsc{CATEGORIA} & -- \\
\bottomrule
\end{longtable}

\subsection{Analisi delle ridondanze}
L'attributo \texttt{Totale\_Speso\_Attuale} della relazione \textsc{BUDGET} è ridondante perché può essere ricavato sommando le spese presenti in \textsc{TRANSIZIONE}. La ridondanza viene mantenuta per ridurre il costo delle letture più frequenti della dashboard.

La coerenza viene garantita con due regole applicative:
\begin{itemize}
    \item quando viene inserita una spesa, l'inserimento della transazione, l'associazione di tag/documento e l'aggiornamento degli eventuali budget avvengono nella stessa transazione JDBC;
    \item quando un budget viene creato o aggiornato dopo spese già registrate, il valore iniziale viene calcolato con una query di aggregazione sulle transazioni esistenti.
\end{itemize}

In questo modo il budget rimane opzionale senza perdere consistenza: l'assenza di un budget non blocca la registrazione della spesa, mentre l'esistenza di un budget impone la sincronizzazione del suo totale ridondante.

\subsection{Raffinamento dello schema}
\subsubsection{Eliminazione delle gerarchie}
La gerarchia spesa/entrata viene eliminata collassando i sottotipi in \textsc{TRANSIZIONE}. La distinzione rimane espressa da \texttt{TipoTransazione}, mentre i vincoli CHECK controllano la presenza corretta di categoria o fonte.

\subsubsection{Eliminazione degli attributi composti}
Non sono presenti attributi composti significativi da scomporre ulteriormente. Nome e cognome dell'utente sono già separati; il periodo è rappresentato da mese e anno nella tabella \textsc{PERIODO}.

\subsubsection{Gestione degli attributi multivalore}
I tag associati a una spesa rappresentano un attributo multivalore. Vengono tradotti nella relazione \textsc{SPESA\_TAG}, con chiave primaria composta da \texttt{ID\_Transizione} e \texttt{ID\_Tag}.

\subsubsection{Scelta delle chiavi primarie}
L'email è scelta come chiave primaria di \textsc{UTENTE}, perché identifica direttamente la sessione applicativa. Le altre entità principali utilizzano identificatori numerici auto-incrementali per semplificare riferimenti e join.

\subsubsection{Traduzione delle associazioni}
Le associazioni 1:N sono tradotte tramite chiavi esterne. Le associazioni N:M, come quella tra spese e tag, sono tradotte tramite tabella ponte. I vincoli applicativi più specifici sono rafforzati tramite trigger e controlli SQL.

\subsection{Schema relazionale finale}
Lo schema relazionale finale comprende le relazioni \textsc{UTENTE}, \textsc{PERIODO}, \textsc{CATEGORIA}, \textsc{FONTE}, \textsc{TAG}, \textsc{TRANSIZIONE}, \textsc{SPESA\_TAG}, \textsc{DOCUMENTO}, \textsc{BUDGET} e \textsc{SPESA\_RICORRENTE}. La struttura è coerente con lo script ufficiale \texttt{doc/sql/schema\_completo.sql}, che definisce chiavi primarie, chiavi esterne, vincoli CHECK e indici.

\subsection{Vincoli di integrità}
I vincoli principali sono:
\begin{itemize}
    \item integrità referenziale tra transazioni e utenti;
    \item vincoli CHECK su ruolo, mese, anno, importo e tipo transazione;
    \item vincolo UNIQUE su \textsc{PERIODO} per la coppia mese/anno;
    \item vincolo UNIQUE su \textsc{BUDGET} per evitare duplicati sullo stesso ambito;
    \item vincoli di proprietà sugli elementi personali.
\end{itemize}

\subsection{Controllo della normalizzazione}
Le relazioni principali rispettano la terza forma normale: gli attributi dipendono dalla chiave, da tutta la chiave e non da attributi non chiave. L'unica ridondanza intenzionale è \texttt{Totale\_Speso\_Attuale} in \textsc{BUDGET}, documentata e controllata tramite logica applicativa.

\section{Implementazione SQL}
\subsection{Script di creazione del database}
Lo script principale crea il database MySQL, definisce le tabelle, imposta chiavi primarie e chiavi esterne e aggiunge vincoli CHECK. Gli script separati gestiscono popolamento iniziale, trigger, viste e query operative.

\subsection{Creazione delle tabelle}
Le tabelle sono create partendo dalle entità indipendenti, come \textsc{UTENTE} e \textsc{PERIODO}, per poi introdurre le tabelle dipendenti. Questa sequenza evita problemi con chiavi esterne non ancora disponibili.

\subsection{Definizione di chiavi primarie e chiavi esterne}
Ogni tabella ha una chiave primaria esplicita. Le chiavi esterne usano azioni coerenti con il dominio: ad esempio la cancellazione di un utente elimina i suoi dati personali, mentre l'eliminazione di un periodo già usato è limitata per preservare la storia dei budget.

\subsection{Vincoli NOT NULL, UNIQUE, CHECK}
I vincoli NOT NULL garantiscono la presenza degli attributi essenziali. I vincoli UNIQUE evitano duplicati su periodo e budget. I vincoli CHECK formalizzano regole di dominio come tipo transazione, mese valido, importo positivo e coerenza tra categoria/fonte.

\subsection{Trigger, procedure o viste}
I trigger presenti negli script rafforzano vincoli che non sono esprimibili interamente con semplici CHECK, come la validità di categorie, fonti, tag e documenti rispetto all'utente proprietario e al tipo della transazione. Le viste semplificano interrogazioni frequenti, come il riepilogo dei movimenti e lo stato dei budget.

\subsection{Popolamento iniziale del database}

\subsection{Query SQL delle operazioni principali}
\subsubsection{Problemi e criticità}
Le query devono garantire:
\begin{itemize}
    \item isolamento utente tramite filtro sull'email;
    \item atomicità delle operazioni complesse;
    \item aggiornamento coerente delle ridondanze;
    \item protezione dalla divisione per zero nelle statistiche;
    \item validità dei documenti rispetto alle sole spese dell'utente.
\end{itemize}

\subsubsection{Inserimento atomico di una spesa}
\begin{lstlisting}[caption={Inserimento di una spesa con periodo, tag e budget}]
START TRANSACTION;

INSERT INTO PERIODO (Mese, Anno)
VALUES (?, ?)
ON DUPLICATE KEY UPDATE ID_Periodo = LAST_INSERT_ID(ID_Periodo);

INSERT INTO TRANSIZIONE
(TipoTransazione, Importo, Data, Descrizione, Email, ID_Categoria, ID_Periodo, ID_Fonte)
VALUES ('S', ?, ?, ?, ?, ?, LAST_INSERT_ID(), NULL);

SET @id_spesa = LAST_INSERT_ID();

INSERT INTO SPESA_TAG (ID_Transizione, ID_Tag)
VALUES (@id_spesa, ?);

UPDATE BUDGET
SET Totale_Speso_Attuale = Totale_Speso_Attuale + ?
WHERE Email = ? AND ID_Periodo = ? AND ID_Categoria IS NULL;

UPDATE BUDGET
SET Totale_Speso_Attuale = Totale_Speso_Attuale + ?
WHERE Email = ? AND ID_Periodo = ? AND ID_Categoria = ?;

COMMIT;
\end{lstlisting}

\subsubsection{Upsert del budget}
\begin{lstlisting}[caption={Upsert del budget con totale iniziale coerente}]
SELECT COALESCE(SUM(Importo), 0) AS Totale_Speso
FROM TRANSIZIONE
WHERE Email = ?
  AND ID_Periodo = ?
  AND TipoTransazione = 'S'
  AND (? IS NULL OR ID_Categoria = ?);

INSERT INTO BUDGET
(Importo_Limite, Alert_Soglia, Totale_Speso_Attuale, ID_Periodo, ID_Categoria, Email)
VALUES (?, ?, ?, ?, ?, ?)
ON DUPLICATE KEY UPDATE
Importo_Limite = VALUES(Importo_Limite),
Alert_Soglia = VALUES(Alert_Soglia),
Totale_Speso_Attuale = VALUES(Totale_Speso_Attuale);
\end{lstlisting}

\subsubsection{Documenti dell'utente}
\begin{lstlisting}[caption={Documenti associati alle spese dell'utente}]
SELECT D.ID_Documento, D.ID_Transizione, D.Path_File, D.Tipo_File,
       D.Data_Acquisizione_Documento,
       T.Data, T.Importo, T.Descrizione
FROM DOCUMENTO D
JOIN TRANSIZIONE T ON D.ID_Transizione = T.ID_Transizione
WHERE T.Email = ?
  AND T.TipoTransazione = 'S'
ORDER BY T.Data DESC, D.ID_Documento DESC;
\end{lstlisting}

\begin{lstlisting}[caption={Spese dell'utente ancora prive di documento}]
SELECT T.ID_Transizione, T.Data, T.Importo, T.Descrizione
FROM TRANSIZIONE T
LEFT JOIN DOCUMENTO D ON D.ID_Transizione = T.ID_Transizione
WHERE T.Email = ?
  AND T.TipoTransazione = 'S'
  AND D.ID_Documento IS NULL
ORDER BY T.Data DESC, T.ID_Transizione DESC;
\end{lstlisting}

\subsubsection{Analisi percentuale}
\begin{lstlisting}[caption={Rapporto spese/entrate mensile}]
SELECT
    (
        SUM(CASE WHEN TipoTransazione = 'S' THEN Importo ELSE 0 END)
        /
        NULLIF(SUM(CASE WHEN TipoTransazione = 'E' THEN Importo ELSE 0 END), 0)
    ) * 100 AS Percentuale_Spese_Su_Entrate
FROM TRANSIZIONE
WHERE Email = ? AND ID_Periodo = ?;
\end{lstlisting}

\subsubsection{Query di supporto}
\begin{lstlisting}[caption={Categorie disponibili per utente}]
SELECT ID_Categoria, Nome, is_system
FROM CATEGORIA
WHERE is_system = TRUE OR Email_Proprietario = ?
ORDER BY is_system DESC, Nome;
\end{lstlisting}

\begin{lstlisting}[caption={Riepilogo transazioni per periodo}]
SELECT T.ID_Transizione, T.Importo, T.Data, T.Descrizione,
       COALESCE(C.Nome, F.Nome) AS Categoria_Fonte,
       CASE WHEN T.TipoTransazione = 'S' THEN 'Spesa' ELSE 'Entrata' END AS Tipo
FROM TRANSIZIONE T
LEFT JOIN CATEGORIA C ON T.ID_Categoria = C.ID_Categoria
LEFT JOIN FONTE F ON T.ID_Fonte = F.ID_Fonte
WHERE T.Email = ? AND T.Data BETWEEN ? AND ?
ORDER BY T.Data DESC;
\end{lstlisting}

\begin{lstlisting}[caption={Budget con residuo e stato di superamento}]
SELECT ID_Budget, Mese, Anno, Ambito,
       Importo_Limite, Totale_Speso, Residuo, Superato
FROM v_budget_stato
WHERE Email = ?
ORDER BY Anno DESC, Mese DESC, Ambito;
\end{lstlisting}

\subsubsection{Motivazioni}
L'uso di \texttt{PreparedStatement} evita concatenazioni insicure e permette di parametrizzare email, date e importi. L'uso di transazioni JDBC garantisce atomicità sulle operazioni che coinvolgono più tabelle. Le query sui documenti attraversano sempre \textsc{TRANSIZIONE}, così il filtro sull'utente rimane centralizzato.

\section{Progettazione dell’applicazione}
\subsection{Architettura generale dell’applicazione}

\subsection{Tecnologie usate}

\subsection{Struttura dei package Java}

\subsection{Collegamento al database tramite JDBC}

\subsection{Organizzazione delle classi principali}

\subsection{Gestione degli errori e validazione input}

\subsection{Interfaccia grafica Java Swing}

Per l'utente standard la dashboard permette di consultare riepilogo, transazioni, budget e classificazioni. Il pulsante per la nuova transazione apre un form applicativo che consente di inserire una spesa o un'entrata scegliendo categoria o fonte tra quelle disponibili per l'utente. La sezione \textit{Categorie e Tag} consente inoltre di aggiungere, rinominare ed eliminare elementi personali, mentre gli elementi di sistema restano protetti e non modificabili dall'utente.

Per l'amministratore viene mostrata una vista distinta, coerente con i requisiti di privacy: l'admin non consulta il dettaglio dei movimenti personali, ma accede a statistiche aggregate mensili e allo stato complessivo dei budget tramite viste SQL dedicate.

La schermata iniziale gestisce sia l'accesso sia la creazione di un nuovo account studente. La registrazione richiede nome, cognome, email, password e conferma password; il nuovo utente viene inserito nella tabella \textsc{UTENTE} con ruolo \texttt{UTENTE}, mentre la password viene salvata in forma hash tramite \texttt{SHA2}. La creazione di account amministrativi non è esposta dall'interfaccia utente, così da mantenere separati gli account operativi dagli account di gestione.

\subsection{Schermate principali}

\subsection{Flussi d’uso principali}

Registrazione studente\\
Inserimento di una spesa\\
Inserimento di una entrata\\
Definizione di un budget\\
Gestione di categorie e tag personali\\
Gestione di una spesa ricorrente\\
Associazione di un documento digitale\\
Visualizzazione statistiche amministratore

\section{Test e validazione}
\subsection{Dati di test utilizzati}
I dati di test includono due studenti normali e un amministratore. Lo studente \texttt{studente1@mail.com} ha transazioni, categorie personali, fonti, tag, budget e almeno un documento dimostrativo. Lo studente \texttt{studente2@mail.com} consente di verificare l'isolamento dei dati.

\subsection{Test delle query SQL}
\begin{longtable}{p{4.5cm} p{5cm} p{5cm}}
\toprule
\textbf{Test} & \textbf{Input} & \textbf{Risultato atteso} \\
\midrule
\endfirsthead
\toprule
\textbf{Test} & \textbf{Input} & \textbf{Risultato atteso} \\
\midrule
\endhead
Login valido & \texttt{studente1@mail.com}, \texttt{password1} & Utente trovato. \\
Login non valido & Password errata & Nessun utente trovato. \\
Documenti utente & Email studente 1 & Solo documenti delle sue spese. \\
Spese senza documento & Email studente 1 & Solo spese prive di documento. \\
Budget stato & Email studente 1 & Budget ordinati per periodo e ambito. \\
Isolamento utenti & Email studente 2 & Nessun dato personale dello studente 1. \\
\bottomrule
\end{longtable}

\subsection{Test delle funzionalità applicative}
\begin{itemize}
    \item avvio dell'applicazione da \texttt{Main};
    \item login con credenziali demo;
    \item apertura della dashboard personale;
    \item visualizzazione della sezione documenti;
    \item apertura di un documento con path valido;
    \item gestione di path non valido con messaggio di errore;
    \item inserimento di un documento su una spesa senza documento.
\end{itemize}

\subsection{Casi limite gestiti}
\begin{itemize}
    \item assenza di budget per il periodo corrente;
    \item assenza di documenti per un utente;
    \item file documento rimosso dal disco;
    \item assenza di entrate nel calcolo percentuale;
    \item tentativo di associare un documento a una spesa non appartenente all'utente.
\end{itemize}

\subsection{Errori noti o limitazioni}
La principale limitazione riguarda i documenti: il DB conserva il percorso del file, non il contenuto. Se il file viene spostato, cancellato o se l'utente usa un altro dispositivo, il percorso può risultare non valido. Questa scelta è consapevole e motivata dalla semplicità del progetto e dal minor carico sul database.

\section{Conclusioni}
\subsection{Risultato ottenuto}
Il progetto realizza una base di dati relazionale coerente con il dominio della gestione economica personale. L'applicazione Java consente di autenticare un utente, mantenere una sessione, interrogare dati filtrati per email e visualizzare le principali aree funzionali.

\subsection{Scelte progettuali principali}
Le scelte più rilevanti sono il collasso della gerarchia spesa/entrata in \textsc{TRANSIZIONE}, l'isolamento dati tramite email, la modellazione ibrida di elementi globali/personali, la ridondanza controllata del budget e la memorizzazione dei documenti tramite path.

\subsection{Possibili sviluppi futuri}
Sviluppi futuri includono:
\begin{itemize}
    \item sincronizzazione o copia controllata dei documenti in una cartella applicativa;
    \item eventuale uso di \texttt{LONGBLOB} per rendere i documenti portabili attraverso il solo database;
    \item completamento delle funzionalità amministrative;
    \item grafici statistici più avanzati;
    \item esportazione dei report in PDF o CSV.
\end{itemize}

\appendix
\section{Script SQL completo}
Gli script SQL sono consolidati nella cartella \texttt{doc/sql}: \texttt{01\_schema\_completo.sql} crea schema, indici, trigger e viste; \texttt{02\_popolamento\_demo.sql} inserisce i dati dimostrativi; \texttt{03\_migrazione\_database\_esistente.sql} aggiorna installazioni precedenti; \texttt{04\_query\_operazioni.sql} raccoglie le query operative facoltative.

\section{Query SQL complete}
Le query principali sono riportate nel capitolo di implementazione SQL e nel file \texttt{doc/sql/04\_query\_operazioni.sql}. Le query applicative usano parametri JDBC e filtrano sempre i dati personali tramite email.

\section{Screenshot dell'applicazione}
Gli screenshot dell'applicazione possono essere inseriti in questa sezione durante la consegna finale. Le schermate più rilevanti sono login, dashboard, budget e gestione documenti.

\section{Manuale utente sintetico}
Per usare l'applicazione occorre avviare \texttt{it.unibo.sage.Main}, effettuare il login con un utente presente nel database e navigare tra le sezioni della dashboard. I dati demo includono l'utente \texttt{studente1@mail.com} con password \texttt{password1}.

\section{Eventuale link al repository}
Il link al repository può essere aggiunto in fase di consegna, insieme a eventuali istruzioni di compilazione ed esecuzione.

\end{document}
